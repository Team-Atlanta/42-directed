---
phase: 03-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - oss-crs/dockerfiles/runner.Dockerfile
  - oss-crs/bin/runner.sh
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-06

must_haves:
  truths:
    - "Runner container builds successfully using target_base_image"
    - "Runner downloads build artifacts from slicer/builder phase"
    - "Runner validates OSS_CRS_TARGET_HARNESS environment variable"
    - "Runner fails with clear error if harness not found or env var not set"
  artifacts:
    - path: "oss-crs/dockerfiles/runner.Dockerfile"
      provides: "Runner container definition with libCRS"
      contains: "COPY --from=libcrs"
    - path: "oss-crs/bin/runner.sh"
      provides: "Runner entrypoint with artifact download and harness validation"
      exports: ["parse_cpuset"]
  key_links:
    - from: "oss-crs/dockerfiles/runner.Dockerfile"
      to: "oss-crs/bin/runner.sh"
      via: "CMD directive"
      pattern: 'CMD.*runner\.sh'
    - from: "oss-crs/bin/runner.sh"
      to: "libCRS download-build-output"
      via: "artifact download"
      pattern: "libCRS download-build-output build"
---

<objective>
Create the runner container foundation that downloads build artifacts via libCRS and validates the target harness.

Purpose: Establish the runner container following the same pattern as builder.Dockerfile, with proper libCRS integration and strict harness validation per user decision.

Output: Functional runner.Dockerfile and runner.sh that can download artifacts and validate the harness exists before proceeding to AFL++ execution (Plan 03-02).
</objective>

<execution_context>
@/home/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/home/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-run/03-CONTEXT.md
@.planning/phases/03-run/03-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase for executor reference -->

From oss-crs/dockerfiles/builder.Dockerfile:
```dockerfile
ARG target_base_image
FROM ${target_base_image}

COPY --from=libcrs . /opt/libCRS
RUN /opt/libCRS/install.sh

COPY oss-crs/bin/builder.sh /builder.sh
RUN chmod +x /builder.sh

CMD ["/builder.sh"]
```

From oss-crs/bin/builder.sh (relevant patterns):
```bash
#!/bin/bash
set -e

# Environment variable defaults
PROJECT_NAME="${PROJECT_NAME:-$(basename $SRC)}"
SRC_DIR="${SRC:-/src}"
OUT_DIR="${OUT:-/out}"

# Artifact download
libCRS download-build-output slice "$SLICE_DIR"

# Strict validation pattern
if [ ! -f "$EXPECTED_FILE" ]; then
    echo "[builder] ERROR: File not found"
    ls -la "$DIRECTORY"
    exit 1
fi
```

From oss-crs/crs.yaml:
```yaml
crs_run_phase:
  fuzzer:
    dockerfile: oss-crs/dockerfiles/runner.Dockerfile
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runner.Dockerfile with libCRS</name>
  <files>oss-crs/dockerfiles/runner.Dockerfile</files>
  <action>
Replace the stub runner.Dockerfile with a proper implementation following builder.Dockerfile pattern:

1. Use `ARG target_base_image` and `FROM ${target_base_image}` for OSS-CRS compatibility
2. Install libCRS via `COPY --from=libcrs . /opt/libCRS` and `RUN /opt/libCRS/install.sh`
3. Copy runner.sh script: `COPY oss-crs/bin/runner.sh /runner.sh`
4. Set executable: `RUN chmod +x /runner.sh`
5. Set entrypoint: `CMD ["/runner.sh"]`

Include comment header explaining this is the directed fuzzer runner.
  </action>
  <verify>
    <automated>grep -q "COPY --from=libcrs" oss-crs/dockerfiles/runner.Dockerfile && grep -q "ARG target_base_image" oss-crs/dockerfiles/runner.Dockerfile && grep -q "CMD.*runner.sh" oss-crs/dockerfiles/runner.Dockerfile && echo "PASS"</automated>
  </verify>
  <done>runner.Dockerfile exists with libCRS installation and runner.sh entrypoint</done>
</task>

<task type="auto">
  <name>Task 2: Create runner.sh with artifact download and harness validation</name>
  <files>oss-crs/bin/runner.sh</files>
  <action>
Create runner.sh entrypoint script with:

1. **Shebang and strict mode**: `#!/bin/bash` and `set -e`

2. **Logging prefix**: Use `[runner]` prefix for all output messages (matching builder.sh pattern)

3. **Environment validation** (STRICT per user decision):
   - Check `OSS_CRS_TARGET_HARNESS` is set, exit 1 with clear error if not
   - Check `OSS_CRS_CPUSET` is set, exit 1 with clear error if not
   - Log both values on startup

4. **Artifact download**:
   - Create `/out` directory if needed
   - Run `libCRS download-build-output build /out`
   - Verify harness exists at `/out/$OSS_CRS_TARGET_HARNESS`
   - If harness not found: print ERROR, list `/out/` contents, exit 1
   - Set executable bit: `chmod +x "/out/$OSS_CRS_TARGET_HARNESS"`

5. **parse_cpuset function** (for Plan 03-02 to use):
   - Parse cpuset format like "4-7,10,12-14" into space-separated core IDs
   - Handle both ranges (4-7) and individual cores (10)
   - Store result in CORES array

6. **Placeholder for AFL++ execution**: Add comment `# AFL++ execution added in Plan 03-02` as marker for next plan

Error messages should be specific and actionable, matching the pattern from builder.sh.
  </action>
  <verify>
    <automated>bash -n oss-crs/bin/runner.sh && grep -q "OSS_CRS_TARGET_HARNESS" oss-crs/bin/runner.sh && grep -q "download-build-output build" oss-crs/bin/runner.sh && echo "PASS"</automated>
  </verify>
  <done>runner.sh exists with artifact download, harness validation, and cpuset parsing function</done>
</task>

</tasks>

<verification>
All verification is automated:
1. `bash -n oss-crs/bin/runner.sh` - syntax check
2. `grep -q "COPY --from=libcrs" oss-crs/dockerfiles/runner.Dockerfile` - libCRS pattern
3. `grep -q "download-build-output build" oss-crs/bin/runner.sh` - artifact download
4. `grep -q "OSS_CRS_TARGET_HARNESS" oss-crs/bin/runner.sh` - harness validation
</verification>

<success_criteria>
- runner.Dockerfile builds following builder.Dockerfile pattern (ARG target_base_image, libCRS install)
- runner.sh validates OSS_CRS_TARGET_HARNESS and OSS_CRS_CPUSET environment variables
- runner.sh downloads build artifacts via `libCRS download-build-output build /out`
- runner.sh fails with clear error messages if harness not found (per user decision)
- parse_cpuset function implemented for AFL++ parallelism (used in Plan 03-02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-run/03-01-SUMMARY.md`
</output>
