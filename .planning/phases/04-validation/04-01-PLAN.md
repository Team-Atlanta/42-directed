---
phase: 04-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - oss-crs/bin/validate.sh
autonomous: false
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "oss-crs prepare phase completes with exit code 0"
    - "oss-crs build-target phase produces allowlist and instrumented harness"
    - "oss-crs run phase starts AFL++ and seeds appear in corpus"
    - "Validation script provides clear pass/fail output for each phase"
  artifacts:
    - path: "oss-crs/bin/validate.sh"
      provides: "End-to-end validation orchestration"
      min_lines: 100
      executable: true
  key_links:
    - from: "oss-crs/bin/validate.sh"
      to: "oss-crs CLI"
      via: "uv run oss-crs prepare/build-target/run commands"
      pattern: "uv run oss-crs"
    - from: "oss-crs/bin/validate.sh"
      to: "oss-crs artifacts"
      via: "JSON artifact path resolution"
      pattern: "oss-crs artifacts"
---

<objective>
Create end-to-end validation script that runs all three OSS-CRS phases (prepare, build-target, run) against the afc-freerdp-delta-01 benchmark and verifies each phase succeeds.

Purpose: Prove the directed fuzzing CRS pipeline works end-to-end with a real benchmark that has a known vulnerability, validating all components built in Phases 1-3.

Output: Executable validate.sh script that runs prepare, build-target, and run phases with clear success/failure reporting.
</objective>

<execution_context>
@/home/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/home/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md

<!-- Key existing patterns from Phase 3 -->
@oss-crs/bin/runner.sh
@oss-crs/crs.yaml

<interfaces>
<!-- From oss-crs CLI (verified via --help) -->
Prepare phase: uv run oss-crs prepare --compose-file PATH
Build-target:  uv run oss-crs build-target --compose-file PATH --fuzz-proj-path PATH --diff PATH [--build-id ID]
Run phase:     uv run oss-crs run --compose-file PATH --fuzz-proj-path PATH --target-harness NAME --timeout SEC --diff PATH [--build-id ID] [--run-id ID]
Artifacts:     uv run oss-crs artifacts --compose-file PATH --fuzz-proj-path PATH --target-harness NAME --build-id ID --run-id ID

<!-- From compose.yaml (exists at ~/post/oss-crs-6/example/42-directed/compose.yaml) -->
run_env: local
docker_registry: local
directed-fuzzer:
  cpuset: "4-7"
  memory: "16G"
  source:
    local_path: ~/post/42-directed

<!-- Benchmark location (verified) -->
Benchmark: ~/post/CRSBench/benchmarks/afc-freerdp-delta-01
Diff file: ~/post/CRSBench/benchmarks/afc-freerdp-delta-01/.aixcc/ref.diff
Harness: TestFuzzCryptoCertificateDataSetPEM
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validate.sh with prepare and build-target phases</name>
  <files>oss-crs/bin/validate.sh</files>
  <action>
Create validation script at oss-crs/bin/validate.sh following existing bin/*.sh patterns.

Script structure:
1. Shebang and set -e for strict error handling
2. Configuration section with defaults:
   - OSS_CRS_DIR="${OSS_CRS_DIR:-$HOME/post/oss-crs-6}"
   - COMPOSE_FILE="${COMPOSE_FILE:-$OSS_CRS_DIR/example/42-directed/compose.yaml}"
   - BENCHMARK_PATH="${BENCHMARK_PATH:-$HOME/post/CRSBench/benchmarks/afc-freerdp-delta-01}"
   - TARGET_HARNESS="TestFuzzCryptoCertificateDataSetPEM"
   - RUN_TIMEOUT=300 (5 minutes per user decision)

3. Input validation:
   - Check compose file exists (exit 1 if not)
   - Check benchmark directory exists (exit 1 if not)
   - Check ref.diff exists (exit 1 if not)

4. Phase 1 - Prepare:
   - cd to OSS_CRS_DIR
   - Run: uv run oss-crs prepare --compose-file "$COMPOSE_FILE"
   - If exit code non-zero: print "[validate] FAILED: Prepare phase failed" and exit 1
   - Print "[validate] PASSED: Prepare phase complete"

5. Phase 2 - Build-target:
   - Generate BUILD_ID="val-$(date +%s)"
   - Run: uv run oss-crs build-target --compose-file "$COMPOSE_FILE" --fuzz-proj-path "$BENCHMARK_PATH" --diff "$BENCHMARK_PATH/.aixcc/ref.diff" --build-id "$BUILD_ID"
   - If exit code non-zero: print failure message with suggestion to check container logs, exit 1
   - Print BUILD_ID for reference
   - Print "[validate] PASSED: Build-target phase complete"

Make script executable with chmod +x.
  </action>
  <verify>
    <automated>test -x /home/andrew/post/42-directed/oss-crs/bin/validate.sh && head -50 /home/andrew/post/42-directed/oss-crs/bin/validate.sh | grep -q "oss-crs prepare"</automated>
  </verify>
  <done>validate.sh exists, is executable, has prepare and build-target phase execution with proper error handling</done>
</task>

<task type="auto">
  <name>Task 2: Add run phase and artifact verification</name>
  <files>oss-crs/bin/validate.sh</files>
  <action>
Extend validate.sh to add run phase execution and post-run verification.

Add Phase 3 - Run section after build-target:
1. Generate RUN_ID="val-$(date +%s)"
2. Run phase with timeout (using || true to continue on timeout):
   ```bash
   uv run oss-crs run \
       --compose-file "$COMPOSE_FILE" \
       --fuzz-proj-path "$BENCHMARK_PATH" \
       --target-harness "$TARGET_HARNESS" \
       --timeout "$RUN_TIMEOUT" \
       --build-id "$BUILD_ID" \
       --run-id "$RUN_ID" \
       --diff "$BENCHMARK_PATH/.aixcc/ref.diff" || true
   ```
3. Print "[validate] Run phase complete (timeout is expected)"
4. Print RUN_ID for reference

Add Verification section after run phase:
1. Get artifact paths via oss-crs artifacts command (capture JSON output)
2. Parse JSON with jq to extract seed directory path
3. Count files in seed directory using find
4. If seeds > 0: print "[validate] PASSED: Corpus growing (N seeds)"
5. If seeds == 0: print "[validate] WARNING: No seeds produced"
6. Check for crashes/POV directory and report count
7. Print final summary: "[validate] VALIDATION COMPLETE" with overall status

Handle jq not installed: fallback to basic path checking if jq unavailable.

Exit 0 on success (all phases completed, at least prepare and build-target passed).
  </action>
  <verify>
    <automated>grep -q "oss-crs run" /home/andrew/post/42-directed/oss-crs/bin/validate.sh && grep -q "VALIDATION COMPLETE" /home/andrew/post/42-directed/oss-crs/bin/validate.sh</automated>
  </verify>
  <done>validate.sh has complete three-phase execution with artifact verification showing seed/crash counts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify validation script runs successfully</name>
  <files>oss-crs/bin/validate.sh</files>
  <action>Human verifies the validation script works end-to-end against the benchmark.</action>
  <verify>
    <automated>MISSING - human verification required</automated>
  </verify>
  <done>User confirms validation passes with all three phases completing successfully</done>
  <what-built>End-to-end validation script for the directed fuzzing CRS</what-built>
  <how-to-verify>
1. Review validate.sh structure:
   ```bash
   cat oss-crs/bin/validate.sh
   ```
   Verify it has: prepare phase, build-target phase, run phase with timeout, artifact verification

2. Run validation (WARNING: takes ~7+ minutes, builds Docker images and runs AFL++):
   ```bash
   cd ~/post/42-directed
   ./oss-crs/bin/validate.sh
   ```

3. Expected output:
   - "[validate] PASSED: Prepare phase complete"
   - "[validate] PASSED: Build-target phase complete"
   - "[validate] Run phase complete (timeout is expected)"
   - "[validate] VALIDATION COMPLETE"
   - Seed count showing corpus growth

4. If any phase fails, check container logs as indicated in error output.
  </how-to-verify>
  <resume-signal>Type "approved" if validation passes, or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
Phase validation complete when:
- [ ] validate.sh exists at oss-crs/bin/validate.sh and is executable
- [ ] Script runs prepare phase with proper compose file path
- [ ] Script runs build-target phase with benchmark path and diff
- [ ] Script runs run phase with 5-minute timeout and target harness
- [ ] Script verifies artifacts (seeds, optionally crashes) after run phase
- [ ] Human verification confirms script runs successfully against benchmark
</verification>

<success_criteria>
1. validate.sh executes all three OSS-CRS phases in sequence
2. Clear pass/fail output for each phase with diagnostic suggestions on failure
3. Run phase time-boxed at 5 minutes (per user decision)
4. Artifact verification shows seed count after run completes
5. User confirms validation passes against afc-freerdp-delta-01 benchmark
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-01-SUMMARY.md`
</output>
